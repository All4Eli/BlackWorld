const fs = require('fs'); let content = fs.readFileSync('src/app/page.js', 'utf8'); content = content.replace('const showGameNav = stage !== \'BOOT\' && isSignedIn;', 'if (stage === \'BOOT\' && isSignedIn && !saveData?.heroData) {\n     setSaveData(prev => ({ ...prev, stage: \'CREATOR\' }));\n     return null;\n  }\n\n  const showGameNav = stage !== \'BOOT\' && isSignedIn;'); fs.writeFileSync('src/app/page.js', content);
